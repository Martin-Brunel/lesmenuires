# Base URL of the Rust API (apps/api). Used both server-side (SSR fetch of the
# booking context) and client-side (booking creation), hence the NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_API_URL=http://localhost:8080
